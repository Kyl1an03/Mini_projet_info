#include "groupe.h"



Groupe::Groupe(const char lettre)
{
	lettre_ = lettre;
	/* recuperation des étudiants du groupe de la lettre à partir du fichier csv
	for (unsigned int i = 0; i < 3; i++)
	{
		Eleve temp;
		tabMembre_.push_back(temp);
	}
	*/
}

